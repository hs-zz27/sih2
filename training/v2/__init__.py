"""Phase 5 architectures: the same tasks, models with enough capacity to learn them.

Nothing in this package replaces anything in `training/`. Every v1 model, its
checkpoint and its published number stay exactly where they are, reachable by
the same code path, because `docs/code-freeze.md` makes a published number
immutable and because a v2 run that fails to beat v1 needs v1 still standing
to say so.
"""
